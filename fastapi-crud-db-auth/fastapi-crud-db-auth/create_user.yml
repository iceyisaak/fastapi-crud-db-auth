info:
  name: create_user
  type: http
  seq: 6

http:
  method: POST
  url: 127.0.0.1:8000/api/v1/auth/signup
  body:
    type: json
    data: |-
      {
        "first_name":"bob",
        "last_name":"green",
        "email":"b.green@company.com",
        "username":"bob",
        "password":"pass123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
