info:
  name: get_all_books
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}books"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
