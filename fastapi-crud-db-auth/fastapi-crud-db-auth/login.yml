info:
  name: login
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseURL}}auth/login"
  body:
    type: json
    data: |-
      {
        "email":"a.smith@company.com",
        "password":"pass123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
