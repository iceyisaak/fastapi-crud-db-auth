info:
  name: vote
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseURL}}vote"
  body:
    type: json
    data: |-
      {
        "post_id":10,
        "dir":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
