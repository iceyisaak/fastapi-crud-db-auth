info:
  name: create_user
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseURL}}users"
  body:
    type: json
    data: |-
      {
        "email":"ann@gmail.com",
        "password":"pass123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
